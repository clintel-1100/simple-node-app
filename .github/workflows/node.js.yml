name: build
on:
  push
    
jobs:
  build:
    name: the job
    runs-on: ${{matrix.os}}
    strategy:
      matrix:
        os: [macos-latest, windows-latest, ubunut-latest]
        node: [10, 12, 14]
        exclude:
          - os: macos-latest
            node: 10
    
    steps:
      - name: my first step
        env:
          MY_VAR: Hello there! My name is
          FIRST_NAME: James
          MIDDLE_NAME: T
          LAST_NAME: Kirk
        run:
          echo $MAR_VAR
          $FIRST_NAME
          $MIDDLE_NAME
          $LAST_NAME
          
          
      
        
        
      
          
        

  
  
  
    
      
  
